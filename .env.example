# Database
DB_PATH=./data/jarvis.db

# API Tokens (comma-separated for multiple tokens)
# Generate secure tokens: openssl rand -hex 32
API_TOKENS=your-personal-token-here,your-agent-token-here

# HTTP Basic Auth (for nginx)
BASIC_AUTH_USER=ash
BASIC_AUTH_PASS=your-secure-password-here
